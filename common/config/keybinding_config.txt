[keys.normal.m]
f = ':sh zwt picker "%{buffer_name}"' # Toggle the left yazi pane and open selections in the Helix pane
g = ':sh zwt git'                     # Open LazyGit
o = ':sh zwt vibe "%{buffer_name}" %{cursor_line}' # Toggle Copilot CLI in a floating pane
t = ':sh zwt terminal'                # Toggle terminal pane at the bottom
