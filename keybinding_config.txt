[keys.normal.m]
f = ':sh thxy picker "%{buffer_name}"' # Toggle the left yazi pane and open selections in the Helix pane
g = ':sh thxy git'
o = ':sh thxy vibe "%{buffer_name}" %{cursor_line}' # Toggle Copilot CLI in a floating pane
